@page
@model Eksamensprojekt_1_semester.Pages.Members.GetAllMembersModel
@{
}
<h1>Medlemmer<</h1>>

<form method="post">
	<div class="input-group mb-3">
		<input asp-for="SearchString" class="form-control mr-1"
			   placeholder="Enter search term" />
		<input type="submit" asp-page-handler="NameSearch"
			   value="Search" class="btn btn-primary" />
	</div>
</form>


<table class="table table-bordered table-hover table-striped">
	<thead>
		<tr>
			<th>
				Id
			</th>
			<th>
				Navn
			</th>
			<th>
				Alder
			</th>
			<th>
				Telefon
			</th>
			<th>
				Email
			</th>
		</tr>
	</thead>
	<tbody>
		@if (Model.Members != null)
		{
			foreach (var member in Model.Members)
			{
				<tr>
					<td>
						@member.Id
					</td>
					<td>
						@member.Name
					</td>
					<td>
						@member.Age
					</td>
					<td>
						@member.PhoneNumber
					</td>
					<td>
						@member.Email
					</td>
					<td>
						<a class="btn btn-primary btn-sm" type="button" title="Edit"
						   asp-page="EditMembers" asp-route-id="@member.Id">Edit</a>
						<a class="btn btn-danger btn-sm" type="button" title="Delete"
						   asp-page="DeleteMembers" asp-route-id="@member.Id">Delete</a>

					</td>

				</tr>
			}
		}
	</tbody>
</table>

<p>
	<a class="btn btn-primary" asp-page="CreateMembers">Add Member</a>
</p>
